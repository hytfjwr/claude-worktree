export type ConfirmOptions = {
  /** Marks the prompt as a destructive action: warning icon, yellow heading. */
  danger?: boolean;
  /** Lines listing what the action affects, shown indented under the heading. */
  details?: string[];
};
